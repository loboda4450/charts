#
# IMPORTANT NOTE
#
# This chart inherits from our common library chart. You can check the default values/options here:
# https://github.com/bjw-s/helm-charts/blob/main/charts/library/common/values.yaml
#

image:
# # -- image repository
  repository: ghcr.io/jeffvli/feishin
# # -- image pull policy
  pullPolicy: IfNotPresent
# # -- image tag
  tag: 1.15.1@sha256:5c0b3a1f307f8d9508c9fd5dcef04349b6cda1740ae95d478498dc86e3e432bf

env:
  {}
# # -- Example configuration for Navidrome
# # SERVER_NAME: Navidrome
# # SERVER_LOCK: true
# # SERVER_TYPE: navidrome
# # SERVER_URL: https://navidrome.example.com

probes:
  liveness:
    enabled: true
    type: HTTP
    path: /
  readiness:
    enabled: true
    type: HTTP
    path: /
  startup:
    enabled: true

service:
  http:
    type: ClusterIP
    controller: feishin
    ports:
      http:
        port: 9180

ingress:
# # -- Further ingress configuration options are available here
# #    [[ref]](https://github.com/bjw-s-labs/helm-charts/blob/common-4.6.2/charts/library/common/values.yaml#L585)
  feishin:
    enabled: false
#   # ingressClassName: nginx-public
#   # annotations:
#   #   cert-manager.io/cluster-issuer: letsencrypt-http01
    hosts: []
#   #   - host: feishin.example.com
#   #     paths:
#   #       - path: /
#   #         service:
#   #           identifier: http
#   #           port: http
    tls: []
#   #   - secretName: feishin-cert
#   #     hosts:
#   #       - feishin.example.com

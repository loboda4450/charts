#
# IMPORTANT NOTE
#
# This chart inherits from our common library chart. You can check the default values/options here:
# https://github.com/bjw-s/helm-charts/blob/main/charts/library/common/values.yaml
#

image:
# # -- image repository
  repository: ghcr.io/juniorjpdj/tgmount
# # -- image pull policy
  pullPolicy: IfNotPresent
# # -- image tag
  tag: latest@sha256:7d5931d4a7b7f0d01abee8f765f6c1845bf2e8677f10a49548fde88463e527f1

args:
# -- Override default command(s) to run
  []

secret:
  {}
# # TGAPP Options:
# # B) tgapp:
# #      tgapp: super_secret_tgapp_token
# # C) tgapp:
# #      existing_secret: super_secret_existing_tgapp_token_secret

persistence:
# # -- Output mount, which will be used to mount the chat to.
  chat:
    accessMode: ReadWriteOnce
    size: 1Gi
    storageClass: external
    advancedMounts:
      tgmount:
        tgmount:
          - path: /app/data/mnt
            readOnly: false
            mountPropagation: Bidirectional

  tgsession:
    accessMode: ReadWriteOnce
    size: 200Mi
    storageClass: external
    advancedMounts:
      tgmount:
        tgmount:
          - path: /app/data
            readOnly: false

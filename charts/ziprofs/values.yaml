#
# IMPORTANT NOTE
#
# This chart inherits from our common library chart. You can check the default values/options here:
# https://github.com/bjw-s/helm-charts/blob/main/charts/library/common/values.yaml
#

image:
# # -- image repository
  repository: ghcr.io/juniorjpdj/ziprofs
# # -- image pull policy
  pullPolicy: IfNotPresent
# # -- image tag
  tag: latest@sha256:d5d841206c2010db30927d3248679e8398d567b047d18c404486f749e16b038a

args:
# -- Override default command(s) to run
  - /app/root
  - /app/mnt
  - -o
  - allowother,foreground,async,cachesize=3000

persistence:
# # -- Input mount, which will be used to mount the source directory
  input:
    accessMode: ReadOnlyMany
    size: 1Gi
    storageClass: external
    advancedMounts:
      ziprofs:
        ziprofs:
          - path: /app/root
            readOnly: true

# # -- Output mount, which will be used to mount the destination directory (e.g. ZipROFSed input)
  output:
    accessMode: ReadWriteOnce
    size: 1Gi
    storageClass: external
    advancedMounts:
      ziprofs:
        ziprofs:
          - path: /app/mnt
            readOnly: false
            mountPropagation: Bidirectional
